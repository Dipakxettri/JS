
const USERNAME = "deepak";
const PASSWORD = 1234;

let username = prompt("Enter userame");
let password = prompt("Enter password");

console.log(username == USERNAME && password == PASSWORD? `You are loggined in`:`Invalid Username or password`);
